{
    "name": "ankifx",
    "version": "1.0.0",
    "description": "AnkiFX Visual Engine",
    "main": "src/index.js",
    "scripts": {
        "build": "node build.js",
        "build:local": "node build.js --local",
        "watch": "node build.js --watch",
        "watch:local": "node build.js --watch --local"
    },
    "devDependencies": {
        "esbuild": "^0.27.4"
    },
    "dependencies": {
        "funkymed-flod-module-player": "^1.0.0"
    }
}
